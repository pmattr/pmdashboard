# Simple Sales Dashboard

A compact Streamlit dashboard with generated sample sales data, sidebar filters, KPI metrics, charts, and a filtered table.

## Run

```powershell
pip install -r requirements.txt
streamlit run app.py
```
